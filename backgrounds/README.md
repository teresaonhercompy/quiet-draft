# Optional background images

Place any JPG, JPEG, PNG, WebP, GIF, or AVIF image in this folder. When the web host exposes a directory listing, Quiet Draft discovers the files automatically and adds them under **Backgrounds Folder** in the Scene menu.

If the host hides directory listings, use **Add background images** inside Quiet Draft instead.
